spring:
  profiles:
    active:
      Testing 
  application:
    name: ASO_Backend
  datasource:
    url: jdbc:mysql://localhost/proyecto_final?useSSL=false&allowPublicKeyRetrieval=true&characterEncoding=UTF-8
    username: root
    password: metrica123
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: create-drop
  jackson:
    time-zone: Europe/Madrid
    locale: es_ES
    
  servlet:
    multipart:      max-file-size:        20MB
      max-request-size:        20MB
    
logging:
 level:
  org:
   hibernate:
    SQL:
     debug
     
server:
  port: 8080
  
jwt:
  secret: holamundo
  
encription:
  secret: holamundo

files:
  route: proyectoASOFiles